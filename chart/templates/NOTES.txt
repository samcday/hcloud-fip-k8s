The controller is now deployed. In the next few moments, it will begin managing the assignment state of
{{- if .Values.floatingIP.selector }}floating IPs matching the label {{ .Values.floatingIP.selector }}
{{- else }}*ALL* floating IPs available in your account
{{- end }}.