apiVersion: v1
kind: Service
metadata:
  name: {{ include "the-nat-controller.fullname" . }}
  labels:
    {{- include "the-nat-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "the-nat-controller.selectorLabels" . | nindent 4 }}
