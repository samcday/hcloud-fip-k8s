apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "the-nat-controller.fullname" . }}-test-connection"
  labels:
    {{- include "the-nat-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "the-nat-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
